from setuptools import setup, find_packages

setup(
    name="systemd_monitor",
    version="0.1.0",
    description="A Python package for systemd monitoring.",
    author="Your Name",
    packages=find_packages(),
    install_requires=[],
)
